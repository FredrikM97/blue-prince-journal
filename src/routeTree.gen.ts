/* eslint-disable */

// @ts-nocheck

// Compatibility shim: runtime routes are declared manually in src/routes/__root.tsx.
// Keep this file minimal so it does not imply file-based route generation coverage.

import { Route as rootRouteImport } from './routes/__root'

export const routeTree = rootRouteImport
